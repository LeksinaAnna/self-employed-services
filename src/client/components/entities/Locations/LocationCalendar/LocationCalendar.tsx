import React from 'react';
import { Rental } from '../../../../../server/modules/domains/rentals/entities/rental.entity';

interface Props {
    rentals: Rental[];
}

export const LocationCalendar: React.FC<Props> = ({ rentals }) => {
    return (
        <div>

        </div>
    );
};